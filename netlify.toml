[build]
  command = "npm run build"
  publish = "dist"

# Admin SPA: all /admin/* routes serve the admin shell
[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
  NODE_ENV = "production"
